@page
@model appstore.Pages.Price.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Price[0].Value)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Price[0].DataSet)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Price[0].DataUnSet)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Price) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Value)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DataSet)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DataUnSet)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.PriceId">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.PriceId">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.PriceId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>

@page
@model appstore.Pages.Provider.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Provider[0].Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Provider[0].Email)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Provider[0].Address)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Provider) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Email)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Address)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ProviderId">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ProviderId">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ProviderId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
